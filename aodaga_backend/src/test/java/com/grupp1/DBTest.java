package com.grupp1;

import junit.framework.TestCase;

public class DBTest extends TestCase {

  /**
   * What do we want to test here?
   * we want to see that if the correct strings are supplied, a user is created.
   * we want to test what happens if the strings are all empty
   * we want to see test what happens if the strings are Null
   *
   */
  public void testCreateUser() {
  }

}